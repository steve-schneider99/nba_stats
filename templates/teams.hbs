<h1>NBA Stat Tracker</h1>

<h3>List of teams:</h3>
<ul>
  {{#each}}
    <li>{{teamName}}</li>
  {{/each}}
</ul>


<h3>Add a team:</h3>
<form>
  <div class="form-group">
    <label for="teamName">Name of team to add:</label>
    {{input id="teamName" class="form-control" value=teamName}}
  </div>
    <button class="btn btn-primary" {{action 'create'}}>Create</button>
</form>
